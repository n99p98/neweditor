'use client';
import { useForm } from 'react-hook-form';
import { z } from 'zod';
import { zodResolver } from '@hookform/resolvers/zod';
const schema = z.object({ name: z.string().optional(), email: z.string().email(), password: z.string().min(8) });
export function AuthModal({ mode = 'login' }: { mode?: 'login'|'register'|'reset' }) { const { register, handleSubmit, formState:{errors,isSubmitting} } = useForm({ resolver: zodResolver(schema) }); return <div className="panel w-full max-w-md p-6"><h3 className="text-2xl font-semibold capitalize">{mode}</h3><form className="mt-6 space-y-4" onSubmit={handleSubmit(async ()=>{})}>{mode==='register' && <input {...register('name')} placeholder="Name" className="w-full rounded-2xl bg-white/10 px-4 py-3" />}<input {...register('email')} placeholder="Email" className="w-full rounded-2xl bg-white/10 px-4 py-3" /><input type="password" {...register('password')} placeholder="Password" className="w-full rounded-2xl bg-white/10 px-4 py-3" />{Object.values(errors)[0] && <p className="text-sm text-rose-300">{Object.values(errors)[0]?.message as string}</p>}<button className="w-full rounded-2xl bg-cyan-400 px-4 py-3 font-medium text-slate-950" disabled={isSubmitting}>{isSubmitting ? 'Please wait...' : 'Continue'}</button><button type="button" className="w-full rounded-2xl border border-white/10 px-4 py-3">Continue with Google</button></form></div>; }
